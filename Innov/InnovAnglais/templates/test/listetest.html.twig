{% extends 'base.html.twig' %}
{% block title %}{{ parent()}} Liste des test {% endblock %}
{% block contenu %}
    <h1> Liste des test </h1>
    {% if not liste is empty %}
        {{ form_start(form)}} 
    <table class="table">
    <tr><th>niveau</th><th>Theme</th><th>Modifier</th><th>Supprimer</th></tr> 
    {% for key,value in liste %}
<tr><td>{{value.niveau}}</td><td>{{value.theme.libelle}}</td><td>  <div  class="btn-group  btn-group-lg"><a href="{{ path('test_modifier',{'id':value.id})}}"><span
class="glyphicon  glyphicon-pencil"  aria-hidden="true"></span></a></div></td><td><input type="checkbox" name="cocher[]" value="{{ value.id }}"/></td></tr> 
    {% endfor %}
    </table>
    {{ form_end(form)}}
{% else %}
    Aucun test à afficher
{% endif %}
{% endblock %}